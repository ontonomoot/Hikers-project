const router = require('express').Router();

// router
//   .get('/place/:id/tasks', (req, res) => {
//     const { id } = req.params;
//     const userId = req.session.user.id;
//     try {

//     } catch (err) {}
//   })

module.exports = router;
